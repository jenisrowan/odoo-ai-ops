[pytest]
# Make this directory pytest's rootdir so test/conftest module names are
# top-level and pytest never imports the parent Odoo package
# (custom_addons/odoo_ai_ops/__init__.py imports `odoo`, unavailable here).
addopts = --import-mode=importlib
