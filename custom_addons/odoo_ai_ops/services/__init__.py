# -*- coding: utf-8 -*-
# Thin HTTP client helpers used by the AI Ops models/controllers.
# These are intentionally NOT Odoo models, so they are imported directly
# (e.g. ``from odoo.addons.odoo_ai_ops.services.agent_client import AgentClient``)
# rather than through the models package.
